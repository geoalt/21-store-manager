const validadeName = require('./validateName');
const validadeProductId = require('./validateProductId');
const validadeProductQuantity = require('./validadeProductQuantity');

module.exports = {
  validadeName,
  validadeProductId,
  validadeProductQuantity,
};